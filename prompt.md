# User Prompts History

This document contains the chronological record of all user requests and prompts shared during the development and integration of the ABCD-Demo application (Flutter & React Native AAR integration).

---

### 1. Add "All Tests (Native)" Button in Flutter
> "see the image. in the flutter app home screen after the contact button add another button 'All Tests (Native)'"

---

### 2. Generate .AAR from React Native (`abcd_rn`)
> "now create properly .AAR in react native project abcd_rn but make sure do the generation properly without any error. I know u are a senior developer please do the generation properly"

---

### 3. Verify and Test .AAR Integration
> "test the .aar file is it work or not"

---

### 4. Provide Testing Credentials
> "login credential  
> email: sumit@yopmail.com  
> password: Sumit@123456"

---

### 5. Create Step-by-Step Integration Guide
> "create one .md file in the ABCD-Demo folder there write all the steps that how to generate the .aar file in react native full steps point wise with short deatils. the how to integarte the .aar file in flutter project with full stetps point wise so the client will be understand prperly. please do it step by step"

---

### 6. Enhance Integration Documentation with Detailed CLIs
> "the documet is not clear. you need to mentions all the clis hoe to generate the aar all all...properly"

---

### 7. Create APK for Client Sharing
> "every thing is fine now. now i want to create apk so i can share to client."

---

### 8. Clean Release Builds
> "now clear all release builds"

---

### 9. Implement Single Sign-On (SSO) between Flutter & React Native
> "i noticed that u are doing multiple times login  
> 1st time in flutter login  
> 2nd time when clicking Test (Native) button then opening React native app and again showing the login screen and again login by user credential same.  
> 
> but client do not want like that.  
> 
> login should be only one time via fluter and when clicking the Test(Native) button then directly update the authtoken in the React native store and directly open the Test list from React Native.  
> 
> credential also same.  
> 
> U are the senior developer . Please undersatnd the exact expectation and do the same exactly .  
> 
> credential for testing  
> email: sumit@yopmail.com  
> password: Sumit@123456"

---

### 10. Fix Header Top Bar Red Color in Test List Screen
> "see the home the yop bar red. but in the test list screen the design damaged the colour red not showing"

---

### 11. Refactor StatusBar & Container in TestDetailScreen
> "fix the abcd_rn/src/screens/test/TestDetailScreen.tsx  
> issue update the `<StatusBar barStyle="light-content" backgroundColor="#B71234" />` StatusBar style like the below code for your suggestiion  
> `<View style={[styles.container, { backgroundColor: "#B71234"}, ]} > <StatusBar barStyle="light-content" />`"

---

### 12. Fix TypeScript/Rendering Issue in TestDetailScreen
> "abcd_rn/src/screens/test/TestDetailScreen.tsx  
> fix the issue in the file"

---

### 13. Integrate Logout API Endpoint
> "export const logoutApi = '/logout';  
> this is the logout end point do integrate properly as per my architecture."

---

### 14. Resume Execution after Pausing
> "please execute again after that when i stopped"

---

### 15. Cross-Platform Logout Integration (Flutter & React Native)
> "integrate the logout in flutter app as well so i logout from flutter then both will be logout flutter and react native both properly"

---

### 16. Build Final Flutter Release APK for Client
> "now create flutter build apk so i can share to client"

---

### 17. Generate Prompts History Markdown Document
> "plese share all the prompts what is shaed t you please do create a promt.md where u listed all my promts point wise and add in the root folder"
